import styled from "styled-components";

export const StyledUl = styled.ul`
  list-style: none;
  display: flex;
  gap: 8rem;

  li {
    font: 500 1.4rem var(--ff-poppins);
    color: var(--clr-gray);
  }

`;
